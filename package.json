{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "locally": "set MONGODB_APP_NAME=gha-demo && set MONGODB_CLUSTER_ADDRESS=us-east-1-cluster.5gwkskd.mongodb.net && set MONGODB_USERNAME=mongo-user && set MONGODB_PASSWORD=mongo-83cr3t! && npm start",
    "start": "node app.js",
    "test": "npx playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.18.1",
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.25.1",
    "playwright": "^1.25.1"
  }
}
